<nav class="bg-white py-2 md:py-4" data-controller="header">
  <div class="container px-4 mx-auto md:flex md:items-center">

    <div class="flex justify-between items-center">
      <a href="/" class="font-semibold text-xl md:text-2xl text-indigo-500">🍕 Ranking</a>
      <button class="border border-solid border-gray-600 px-3 py-1 rounded text-gray-600 opacity-50 hover:opacity-75 md:hidden" data-action="click->header#openHamburger">
        <i class="fas fa-bars"></i>
      </button>
    </div>

    <div class="hidden md:flex flex-col md:flex-row md:ml-auto mt-3 md:mt-0" id="navbar-collapse">
      <a href="https://hunt-alternance.com" target="_blank" class="p-2 lg:px-4 md:mx-2 text-indigo-600 text-center border border-transparent rounded hover:bg-indigo-100 hover:text-indigo-700 transition-colors duration-300">🎯 Trouve ton alternance</a>
      <% unless user_signed_in? %>
      <%= link_to 'Connexion', new_user_session_path, class: 'p-2 lg:px-4 md:mx-2 text-indigo-600 text-center border border-transparent rounded hover:bg-indigo-100 hover:text-indigo-700 transition-colors duration-300' %>
      <%= link_to "S'inscrire", new_user_registration_path, class: 'p-2 lg:px-4 md:mx-2 text-white text-center bg-indigo-500 rounded hover:bg-indigo-600 mt-1 md:mt-0 transition-colors duration-300' %>
      <% else %>
      <%= link_to 'Déconnexion', destroy_user_session_path, method: :delete, class: 'p-2 lg:px-4 md:mx-2 text-white text-center bg-indigo-500 rounded hover:bg-indigo-600 mt-1 md:mt-0 transition-colors duration-300' %>
      <% end %>
    </div>
  </div>
</nav>
