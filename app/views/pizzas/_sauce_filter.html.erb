<div class="flex justify-center my-5 md:w-1/4 md:ml-2 lg:w-1/6 lg:ml-0">
  <select class="flex justify-center w-5/6 md:w-full lg:ml-2 border-2 border-gray-300 bg-white rounded-md outline-none focus:outline-none focus:ring focus:ring-purple-400 focus:border-transparent md:text-lg">
    <% sauces.each do |key, value| %>
      <option data-action="click->pizza-filter#filter" value="<%= key %>"><%= value %></option>
    <% end %>
  </select>
</div>
